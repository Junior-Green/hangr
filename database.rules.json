{
  "rules": {
    "users": {
      "$uid": {
        ".read": "$uid === auth.uid",
        ".write": "$uid === auth.uid",
        "is_premium_member": {
          ".validate": "newData.isBoolean()"
        }
      }
    }
  }
}