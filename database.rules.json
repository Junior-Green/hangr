{
  "rules": {
    "users": {
      "$uid": {
        "is_pro_member": {
          ".read": "$uid === auth.uid",
          ".write": "$uid === auth.uid",
          ".validate": "newData.isBoolean()"
        }
      }
    }
  }
}